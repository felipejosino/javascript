var btn = window.document.querySelector('input#btn')
var print = window.document.querySelector('div#print')


function converterTemperatura(){
    window.alert(`Desafio em manuteção...`)

// let c = parseFloat(window.prompt(`Digite uma temperatura em °C (Celsius).  Ex: 35.7 `))

// let k = (c + 273.15).toFixed(2)

// let f = (c * 1.8 + 32).toFixed(2)

// print.innerHTML = 
// `   <h2>A temperatura de ${c}°C, corresponde a...</h2>
//     <p>${k}°K (Kelvin)</p>
//     <p>${f}°F (Fahrenheit)</p>
// `
}

btn.addEventListener('click', function() {
    converterTemperatura()
})